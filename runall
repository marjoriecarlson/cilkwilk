echo "------- BATCH RUN -------" >> results;
date >> results;

for file in data/*;
    do
        echo $file >> results;
        ./nonogram -s < $file >> results;
    	./nonogram < $file >> results;
    done;